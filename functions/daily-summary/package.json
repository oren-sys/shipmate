{
  "name": "daily-summary",
  "version": "1.0.0",
  "description": "Daily admin summary email with orders, revenue, and alerts",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "gcloud functions deploy daily-summary --runtime nodejs20 --trigger-http --allow-unauthenticated --region me-west1 --memory 256MB --timeout 60s"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
