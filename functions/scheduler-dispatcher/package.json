{
  "name": "scheduler-dispatcher",
  "version": "1.0.0",
  "description": "Central scheduler that fans out to ShipMate background tasks",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "gcloud functions deploy scheduler-dispatcher --runtime nodejs20 --trigger-http --allow-unauthenticated --region me-west1 --memory 256MB --timeout 120s"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
